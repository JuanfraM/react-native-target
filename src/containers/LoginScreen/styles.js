import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flex: 1
  },
  image: {
    height: 230,
    position: 'absolute',
    top: 0,
    width: 380
  },
  textCenter: {
    fontSize: 22,
    fontWeight: 'bold',
    left: 120,
    letterSpacing: 3,
    position: 'relative',
    top: 100
  }
});

export default styles;
